@AGENTS.md

Claude Code must also read `D:/urakick/CODEX_CLAUDE.md` before making changes.

## Git ブランチ運用

| 項目 | 内容 |
|------|------|
| デフォルトブランチ | `main` |
| push コマンド | `git push origin main` |
| PR | 不要。`main` への直接 push → Vercel が自動デプロイ |
| `master` ブランチ | 廃止済み（2026-05-04）。使用禁止 |

> 全リポジトリ `main` 統一完了（2026-05-04）。
